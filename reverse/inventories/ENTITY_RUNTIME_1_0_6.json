{
  "build": "Deimos Rising 1.0.6",
  "code_section": 0,
  "routines": {
    "state_action_transition": "0x146F0",
    "range_transition": "0x15280",
    "rule_evaluator": "0x15550",
    "animation_update": "0x15930",
    "entity_destroy": "0x16300",
    "inclusive_integer_random": "0x46580",
    "legacy_rng": "0x553E0",
    "strcmp": "0x57820"
  },
  "state_limits": {
    "minimum_states_per_unit": 1,
    "maximum_states_per_unit": 20,
    "canonical_maximum_states": 14,
    "entry_counter_slots_per_entity": 20,
    "canonical_maximum_counter_threshold": 16
  },
  "timer": {
    "selection": "min == max ? min : min + RNG15 % (max - min + 1)",
    "trigger": "currentTick == stateEntryTick + chosenDelay",
    "canonical_reversed_bound_pairs": 0
  },
  "rng": {
    "state_update": "seed = seed * 1103515245 + 12345 (mod 2^32)",
    "result": "(seed >> 16) & 0x7FFF",
    "result_bits": 15
  },
  "range_transition": {
    "disabled_when": "configured_range == 0.0",
    "trigger": "measured_distance < configured_range",
    "comparison": "strict"
  },
  "rule_execution": {
    "slots_per_state": 5,
    "supported_conditions": 17,
    "ordering": "file order",
    "selection": "first true predicate wins",
    "unresolved_action_effect": "no state/lifetime change; still stops later rules for that tick"
  },
  "state_action_lookup": {
    "comparison": "byte-exact strcmp",
    "delete_token": "Delete",
    "destroy_token": "Destroy",
    "unresolved_nonempty_label": "no-op",
    "canonical_active_unresolved_occurrences": 44,
    "canonical_case_only_noop_occurrences": 15
  }
}
